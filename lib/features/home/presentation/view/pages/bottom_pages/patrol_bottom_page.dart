import 'package:flutter/material.dart';

class PatrolBottomPage extends StatelessWidget {
  const PatrolBottomPage._({Key? key}) : super(key: key);

  static Widget create() => const PatrolBottomPage._();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Patrol bottom page'),
    );
  }
}
