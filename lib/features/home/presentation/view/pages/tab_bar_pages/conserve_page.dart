import 'package:flutter/material.dart';

class ConservePage extends StatelessWidget {
  const ConservePage._({Key? key}) : super(key: key);

  static Widget create() => const ConservePage._();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('conserve page'),
    );
  }
}
